version: '3.9'

services:
  library-app:
    build: ./library
    ports:
      - '3000:3000'
    depends_on:
      - counter-service

  counter-service:
    build: ./counter-service
    ports:
      - '4000:4000'
    volumes:
      - counter-data:/app/data

volumes:
  counter-data:
